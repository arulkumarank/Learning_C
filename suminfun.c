#include <stdio.h>
int main(){
    int a,b,c,d,sum;
    for (int i = 0; i < 4; i++)
    {
        printf("Enter the number : ");
        scanf("%d",&a);

    }
    printf("Sum is %d", sum);
    return 0;
}   